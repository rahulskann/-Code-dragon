export const metadata = {
  title: "CODE DRAGON — Interview of Fire",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
